# Cloudflare Worker secrets (set with: npx wrangler secret put <NAME>)
SCRAPSCACHE_URL="https://scrapscache.com"
# Optional static-vault access only; leave empty for browser OAuth.
SCRAPSCACHE_SYNC_KEY=""
MCP_BEARER_TOKEN=""
# Required independent secret used to seal OAuth values. Use 32+ random bytes.
MCP_SECRET=""
# MCP_FRIENDS_TOKENS='{"token_alice":"syncKeyAlice","token_bob":"syncKeyBob"}'
